extends SubViewportContainer
#Below the list of spawn points inside this scene that you can call from a gateway;
#at index 0 there always common spawn point for each scene, so if a gateway call
#a scene without passing any spawn point index, 0 will be used.
const spawnPoints:=[Vector2(50, 50), Vector2(660, 58)]
